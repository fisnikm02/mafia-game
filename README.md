# Mafia Game

```
in main directory you can find "database.sql"
import database.sql to your phpmyadmin panel
then you can create an user in register form

You can run directly by url localhost/mafia-game

There is no package, all code is pure PHP, Bootstrap CSS, HTML
```
